function asd(one, two , three){
    let a=(one.length*two.length)* three.length/2
    a=Math.ceil(a/2.5)

    console.log(`The plate was rotated ${a} times.`)

}asd('Black Sabbath', 'Paranoid', 'War Pigs')

//(albumName.length * bandName.length) * song-name.length / 2