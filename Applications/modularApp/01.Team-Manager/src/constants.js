export const baseUrl = "http://localhost:3030";

export const baseUsersUrl = `${baseUrl}/users`;
export const baseTeamsUrl = `${baseUrl}/data/teams`;
export const baseMembersUrl = `${baseUrl}/data/members`;
